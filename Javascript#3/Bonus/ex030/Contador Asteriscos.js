// Cria uma funçào que exibe a quantidade de *
// que aquela linha possui

exibirAsteriscos(5);
function exibirAsteriscos(linhas){

    // Loop interno, nestado
    // for(let linha =1; linha <= linhas; linha++){
    //     let padrao = '';
    //     for(let i = 0; i < linha; i++){
    //         padrao += '*';
    //     }
    //     console.log(padrao);

    let padrao = '';
    for(let linha = 1; linha <= linhas; linha++){
        padrao += '*';
        console.log(padrao);
        }
    }


    